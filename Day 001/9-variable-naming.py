#n = "Mario" -> use meaningful names
name = "Mario"

#l = len(name) -> same here
length = len(name)

user_name = "Luigi" # multi word names

#do not use keyword as variable name like: input to avoid confusion